#!/usr/bin/env bash
set -e

ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
source "$ROOT/code/virtualenv/claude/bin/activate"
cd "$(dirname "$0")/../../python/01_struct_skeleton"
python3 examples/example.py
